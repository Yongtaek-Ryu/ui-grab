# Design

## Approach

Use `README.md` as the only maintained top-level documentation file. Since `packages/grab/README.md` is a packaged copy of the root README, remove the same language switcher block there as well so published docs stay consistent with the repository policy.

## Verification

- Confirm no localized root README files remain.
- Confirm no references to deleted translation files remain in the maintained README files.
